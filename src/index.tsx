import React from 'react';
import ReactDOM from 'react-dom';
import { App } from '@brunhild/app';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.querySelector('#app'),
);
